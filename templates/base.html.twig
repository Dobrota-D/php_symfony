<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
        <style>
            body{
                margin: 0;
            }
            .navBar{
                text-align: center;
                background-color: darkgreen;
                color: white;
                width: 100vw;
                font-size: 5vh;
            }
        </style>
        <nav class="navBar">- TriCount -</nav>
        {% block body %}
            <h1>Welcome to TriCount Wish !</h1>
            <a href="{{ path("connect") }}">Connect to your Account</a>
        {% endblock %}
    </body>
</html>
